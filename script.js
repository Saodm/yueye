document.addEventListener('DOMContentLoaded', () => {    const girlImg = document.querySelector('.girl-img');    girlImg.addEventListener('click', createNumbers);});function createNumbers(e) {    const numbers = ['4', '9'];    const count = window.innerWidth < 768 ? 30 : 40;        for (let i = 0; i < count; i++) {        const num = document.createElement('div');        num.className = 'number';        num.textContent = numbers[Math.floor(Math.random() * numbers.length)];                const rect = e.target.getBoundingClientRect();        const centerX = rect.left + rect.width/2;        const centerY = rect.top + rect.height/2;                num.style.left = `${centerX}px`;        num.style.top = `${centerY}px`;                const angle = Math.random() * Math.PI * 2;        const distance = 400 + Math.random() * 300;        const tx = Math.cos(angle) * distance;        const ty = -Math.abs(Math.sin(angle) * distance * 1.2);                num.style.setProperty('--tx', `${tx}px`);        num.style.setProperty('--ty', `${ty}px`);                document.body.appendChild(num);        setTimeout(() => num.remove(), 1200);    }}